[package]
name = "scirust-rsi"
version = "0.1.0"
edition = "2021"
description = "Recursive self-improvement engine — bounded, elitist, reproducible loops (propose → evaluate → keep-if-better → repeat)."
license = "PolyForm-Noncommercial-1.0.0"

[lib]
name = "scirust_rsi"
path = "src/lib.rs"

[dependencies]
rand = { version = "0.8" }
